# Supabase — public (safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://hwuuuslpzxcgpfzdjrhz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (never expose to browser)
SUPABASE_SERVICE_ROLE_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# n8n automation backend (internal, never shown to users)
N8N_WEBHOOK_BASE_URL=https://automatizacion.pacienteia.com

# Internal admin dashboard — access via /analytics/admin?key=<value>
ADMIN_DASHBOARD_SECRET=

# Vercel Cron — auto-injected by Vercel, set manually for local testing
# GET /api/internal/task-automation  →  Authorization: Bearer <value>
CRON_SECRET=

# Anthropic — Copiloto Operativo LLM processing
ANTHROPIC_API_KEY=
